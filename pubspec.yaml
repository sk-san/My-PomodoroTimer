name: my_pomodoro
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.env

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  simple_circular_progress_bar: ^1.0.2
  english_words: ^4.0.0
  provider: ^6.0.2
  flutter_heatmap_calendar: ^1.0.5
  vibration: ^1.8.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  get: ^4.6.1
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
